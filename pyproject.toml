[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Invictus-Microsoft"
authors = [{name = "Invictus-ir", email = "info@invictus-ir.com"}]
dynamic = ["version", "description"]
